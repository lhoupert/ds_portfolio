name: ds_portfolio
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Other (remove and add as needed)
  - jupyter
  - nb_conda_kernels
  - jupyterlab
  - xarray
  - numpy
  - scipy
  - dask
  - netcdf4
  - pandas
  - matplotlib=3.3.0
  - seaborn
  - cartopy
  - scikit-learn
  - imbalanced-learn
